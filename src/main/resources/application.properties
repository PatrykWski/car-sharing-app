spring.datasource.url=jdbc:mysql://localhost:3306/car_sharing?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=validate
spring.liquibase.enabled=true

jwt.secret = ${JWT_SECRET}
jwt.expiration = 3600000

stripe.sk = ${STRIPE_SECRET_KEY}
stripe.url = ${STRIPE_URL}
telegram.token = ${TELEGRAM_TOKEN}
telegram.chatId = ${TELEGRAM_CHAT_ID}
app.manager.password = ${MANAGER_PASSWORD}
app.manager.email = ${MANAGER_EMAIL}
